name: Module CI
on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - main
      - 'release/**'
      - develop
      - 'feature/**'
      - 'wo/**'
  workflow_dispatch:
    inputs:
      comment:
        description: Just a simple comment to know the purpose of the manual build
        required: false

jobs:
  call:
    name: Default CI Flow
    uses: ./.github/workflows/ci_module.yml
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    with:
      SONAR_PROJECT_KEY: openimis_openimis-be-claim_py
      SONAR_ORGANIZATION: openimis-1
      SONAR_PROJECT_NAME: openimis-be-claim_py
      SONAR_PROJECT_VERSION: 1.0
      SONAR_SOURCES: claim
      SONAR_EXCLUSIONS: "pyproject.toml,**/migrations/**,**/static/**,**/media/**,**/tests/**"

