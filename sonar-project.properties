# openIMIS backend modules are libraries: the assembly (openimis-be_py) resolves
# sibling modules and pins versions; a module-local uv.lock/poetry.lock is not used.
sonar.sources=claim
sonar.exclusions=pyproject.toml,**/pyproject.toml,**/migrations/**,**/static/**,**/media/**,**/tests/**
sonar.sca.exclusions=**/pyproject.toml

sonar.issue.ignore.multicriteria=lockfile_s8565
sonar.issue.ignore.multicriteria.lockfile_s8565.ruleKey=text:S8565
sonar.issue.ignore.multicriteria.lockfile_s8565.resourceKey=pyproject.toml
