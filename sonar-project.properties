# openIMIS backend modules are libraries: runtime installs use openimis-be_py assembly
# pinning. uv.lock is committed for SonarCloud SCA (S8565 predictable dependency versions).
sonar.sources=claim
sonar.exclusions=pyproject.toml,**/pyproject.toml,**/migrations/**,**/static/**,**/media/**,**/tests/**
