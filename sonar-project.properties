# Library module: dependencies are resolved by the openIMIS assembly (openimis-be_py),
# not via a module-local lock file. Exclude packaging metadata from security rules
# that require uv.lock/poetry.lock (S8565).
sonar.exclusions=**/pyproject.toml
